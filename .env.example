# Encryption Key for DB Credentials (32 bytes base64 encoded)
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=your_encryption_key_here

# Optional: Default Database Connection (for testing)
# DEFAULT_DB_SID=ORCL_SID1
# DEFAULT_DB_HOST=localhost
# DEFAULT_DB_PORT=1521
# DEFAULT_DB_SERVICE_NAME=ORCL
# DEFAULT_DB_USER=scott
# DEFAULT_DB_PASSWORD=tiger
